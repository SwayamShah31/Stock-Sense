# Admin Panel

This workspace section is reserved for the admin/seller experience.

Planned structure:
- `frontend/` for the admin UI
- `backend/` for the admin API

For now, the admin workspace reuses the live StockSense app that already exists in the root `client/` and `server/` folders.
