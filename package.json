{
  "name": "stock-sense-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "admin/*",
    "user/*",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "dev:admin": "concurrently \"npm run dev --workspace admin/backend\" \"npm run dev --workspace admin/frontend\"",
    "build:admin": "npm run build --workspace admin/frontend",
    "start:admin": "npm run start --workspace admin/backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}