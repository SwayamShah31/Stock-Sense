# User Panel

This workspace section is reserved for the customer-facing storefront.

Planned structure:
- `frontend/` for the user UI
- `backend/` for the user API

This area is scaffolded first so the app can be split cleanly into user and admin lanes.
